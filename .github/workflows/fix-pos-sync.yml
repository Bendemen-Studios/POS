name: Fix POS sync and admin inventory (manual)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time sync patch workflow is disabled; source changes must be applied directly."
